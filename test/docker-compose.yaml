services:
  db:
    image: postgres:14-alpine
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test
    ports:
      - 5433:5432
    volumes:
      - ../configs/sql:/docker-entrypoint-initdb.d